# .husky/pre-commit
#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running Biome check before commit..."
bun run check # package.json に定義した "check" スクリプトを実行
if [ $? -ne 0 ]; then
  echo "Biome check failed. Please fix errors before committing."
  exit 1
fi
echo "Biome check passed."
